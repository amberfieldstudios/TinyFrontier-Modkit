// PropHunt Mod Kit runtime module. Carries the shared prop-tagging type so the same UClass
// is present in both the packaged game and the Mod Kit project.

#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, PropHuntModKitRuntime)
